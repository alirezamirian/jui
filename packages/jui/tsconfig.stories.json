{
  "extends": "./tsconfig.json",
  "compilerOptions": {},
  "include": ["src/**/*.stories.tsx", "src/ForwardRefPatch.d.ts"],
  "exclude": [
    "src/demo.stories.tsx" // FIXME: maybe handle in example app package, separately, although demos can be more than example-app
  ]
}
