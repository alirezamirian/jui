{
  "extends": "../../tsconfig.json",

  // keeping references here helps with editor support. We don't compile this project directly.
  "references": [
    {
      "path": "tsconfig.app.json"
    },
    {
      "path": "tsconfig.jest.json"
    }
  ]
}
