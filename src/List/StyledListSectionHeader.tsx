import { styled } from "../styled";

export const StyledListSectionHeader = styled.li(({}) => ({
  paddingLeft: 8,
  fontWeight: "bold",
  lineHeight: "20px",
  outline: "none",
  cursor: "default",
}));
